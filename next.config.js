/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you copied any custom image domains or settings from your .ts file, paste them here
};

module.exports = nextConfig;